const userData=require('./users');
const petData=require('./pets');
const paymentData=require('./payments');

module.exports=
{
    users: userData,
    pets: petData,
    payments: paymentData,
}